# Clipboard

- `get_clipboard` — recent clipboard history previews (not full secrets)
- `copy_to_clipboard` — `{ text }` copies via `wl-copy`

Do not dump the entire clipboard database. Ask before copying sensitive text.
