# Windows

- `get_windows` — `{ address, title, class_name, focused }`
- `focus_window` — `{ address }` focuses that client

Addresses come from `get_windows`. Do not invent Hyprland `hyprctl` dispatches when these tools exist.
