# Notifications

- `get_notifications` — recent desktop notifications (`app`, `summary`, `body`)
- `notify` — `{ summary, body }` posts an Ambxst[+] notification

Use `notify` for confirmations instead of `notify-send`.
