# AGENTS.md — modules/sidebar/

The full-height AI sidebar is gone. This folder only keeps shared chat widgets used by the Spotlight overlay:

| File | Role |
|------|------|
| `CodeBlock.qml` | Fenced code with copy + syntax highlighting |
| `ModelSelectorPopup.qml` | Model picker popup |

The overlay lives in `modules/widgets/assistant/`.
